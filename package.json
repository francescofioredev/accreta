{
  "name": "accreta-monorepo",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "A knowledge base an agent writes and maintains, with provenance to the sources and drift detection when they change.",
  "workspaces": [
    "packages/*",
    "packages/adapters",
    "packages/adapters/*",
    "bench"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint packages",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "bench": "bun run bench/search-bench.ts",
    "bench:scale": "bun run bench/scale-bench.ts",
    "bench:mcp": "bun run bench/mcp-budget.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "oxlint": "^1.14.0",
    "prettier": "^3.4.2",
    "typescript": "^5.9.3"
  }
}
